filter {
  if [logtype] == "bro_snmp" {
    grok {
      patterns_dir => [ "/etc/logstash/patterns" ] 
      match => [ "message", '\<%{NUMBER:pri}\>%{BSDDATE:date} %{DATA:loghost} %{DATA:logtype}: %{BROSNMPSO}' ]
      add_tag => [ "grokked" ]
      overwrite => [ "date", "logtype" ]
      remove_field => [ "loghost", "message", "pri", "uid" ]
    }
  }
}

